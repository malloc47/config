# This file must be encrypted with sops before it can be used.
# Run: sops secrets/secrets.yaml
# See hosts/aida/secrets.nix for setup instructions.
#
# Required keys (add values when editing with sops):
#
# cloudflare/ddns-token: <Cloudflare API token, Zone:DNS:Edit scope>
# cloudflare/caddy-env: |
#     CLOUDFLARE_API_TOKEN=<same or separate token>
# immich/db-password: <random password, e.g. `openssl rand -base64 32`>
#
# DO NOT commit this file unencrypted. Add a .sops.yaml at the repo root
# and run `sops -e -i secrets/secrets.yaml` to encrypt in place.
